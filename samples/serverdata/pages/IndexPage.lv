import live-elements-web-server.page
import live-web.dom

component IndexPage < Page{
    output: 'dark.html'
    title: 'Index'
    clientRender: body
    entryScript: 'main.bundle.js'

    Body{
        id: body
    }
}