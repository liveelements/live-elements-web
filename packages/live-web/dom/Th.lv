
component Th < DOMElement{

    constructor(text:string){
        super('th')
        Th.prototype.__initialize.call(this)
        
        if ( text )
            this.children = [new T(text)]
    }
}