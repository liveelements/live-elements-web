
component Strong < DOMElement{
    constructor(text:string){
        super('strong')
        Strong.prototype.__initialize.call(this)

        if ( text )
            this.children = [new T(text)]
    }
}