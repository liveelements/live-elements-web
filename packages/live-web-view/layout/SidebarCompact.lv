import live-web.dom
import live-elements-web-server.style

component SidebarCompact < Div{
    string[] extraClasses: []
    classes: this.extraClasses.concat([ScopedStyle.className(SidebarCompact)])
}