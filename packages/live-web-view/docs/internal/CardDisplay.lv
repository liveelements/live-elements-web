import live-web.dom
import live-web
import live-elements-web-server.style

component CardDisplay < Div{
    static string RelativePositioning = 'pos-relative'

    classes: this.extraClasses.concat([ScopedStyle.className(CardDisplay)])
    string[] extraClasses: []
}