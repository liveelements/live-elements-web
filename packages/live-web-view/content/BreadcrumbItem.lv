import live-web.dom
import live-elements-web-server.style

component BreadcrumbItem < Li{
    string[] extraClasses: []
    classes: this.extraClasses.concat([ScopedStyle.className(BreadcrumbItem)])
}
