import live-web.dom
import live-elements-web-server.style

component CollapsibleContent < Div{
    string[] extraClasses: []
    classes: this.extraClasses.concat([ScopedStyle.className(CollapsibleContent)])
}