import live-web.dom

component H2S < H2{

    constructor(text:string){
        super()
        this{}
        if ( text ){
            this.glid = H1S.slugify(text)
            this.children = [new T(text)]
        }
    }
}
