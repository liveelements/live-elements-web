import live-web.dom
import live-elements-web-server.style

component CollapsibleRotate < Div{
    string[] extraClasses: []
    classes: this.extraClasses.concat([ScopedStyle.className(CollapsibleRotate)])
}