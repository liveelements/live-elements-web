import live-web.dom
import live-web
import live-elements-web-server.view
import live-elements-web-server.style

component TextInput < Input{
    type: 'text'

    string[] extraClasses: []
    classes: this.extraClasses.concat([ScopedStyle.className(TextInput), 'text-color', 'text-color-input'])
}