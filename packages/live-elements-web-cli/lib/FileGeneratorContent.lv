component FileGeneratorContent{

    var content: ''

    constructor(content:string){
        super()
        FileGeneratorContent.prototype.__initialize.call(this)
        this.content = content
    }
}
